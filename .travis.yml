language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t roanhennig/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true roanhennig/docker-react npm run test -- --coverage